{
  "name": "mobile-touchpad",
  "version": "1.0.0",
  "description": "App to control your pc from a browser",
  "scripts": {
    "start": "tsc && node dist",
    "build": "tsc",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "addon-input-ctrl": "github:lasicol/addon-input-ctrl#master",
    "express": "^4.17.3",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.2",
    "@types/socket.io": "^2.1.4",
    "typescript": "^3.7.4"
  }
}
